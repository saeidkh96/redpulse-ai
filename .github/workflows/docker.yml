name: Docker Build
on: [pull_request]
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t redpulse-ai:${{ github.sha }} -f backend/Dockerfile .
